import React from "react";
import ReactDOM from "react-dom/client";
import HackDashboard from "./HackDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HackDashboard />
  </React.StrictMode>
);
